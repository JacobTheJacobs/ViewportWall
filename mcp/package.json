{
  "name": "viewport-wall-mcp",
  "version": "1.0.0",
  "description": "Let an AI agent check any page for responsive layout bugs across 57 real device viewports. MCP server and CLI, no setup beyond your installed Chrome.",
  "keywords": ["mcp", "model-context-protocol", "responsive", "testing", "viewport", "devices", "chrome", "agent", "accessibility"],
  "license": "MIT",
  "type": "module",
  "bin": { "viewport-wall-mcp": "src/index.mjs" },
  "files": ["src", "README.md"],
  "engines": { "node": ">=18" },
  "scripts": {
    "sync": "node sync-devices.mjs",
    "test": "node test/run.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "puppeteer-core": "^25.11.0"
  }
}
